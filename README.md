# PyZar Base App

A modular Flask application with user, role, module, and settings management.

## Features
- Role-based module access
- Dynamic settings panel
- Font Awesome icon integration
- Flask-Login support

## Setup

```bash
pip install -r requirements.txt
python run.py
"# mtc" 



SELECT *
FROM ctms1000
WHERE (CREATEDT >= '2026-01-01' AND CREATEDT < '2026-02-01')
   OR (MODIFYDT >= '2026-01-01' AND MODIFYDT < '2026-02-01');


SELECT *
FROM ctms4100
WHERE (CREATEDT >= '2026-01-01' AND CREATEDT < '2026-02-01')
   OR (MODIFYDT >= '2026-01-01' AND MODIFYDT < '2026-02-01');


SELECT *
FROM ctms4000
WHERE (CREATEDT >= '2026-01-01' AND CREATEDT < '2026-02-01');